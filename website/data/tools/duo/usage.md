#### Via CLI

```sh title="Shell"
duo --use duo-babel
```

#### Via Node API

```js title="JavaScript"
Duo(root)
  .entry(entry)
  .use(babel)
  .run(fn);
```

:::info
  <p>
    For more information see the <a href="https://github.com/babel/duo-babel">babel/duo-babel repo</a>.
  </p>
:::

